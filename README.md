# pl-bot diary

Auto-published diary of a Fantasy Premier League bot: what it recommended before each
deadline, and an honest record of how those recommendations turned out.

The bot itself lives in a private repository. This repo contains only generated HTML,
pushed automatically whenever the bot writes to its memory. Mini-league rivals are
anonymised — their names are not mine to publish.

Nothing here is hand-edited. Every commit is machine-generated.
